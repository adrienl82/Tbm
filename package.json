{
  "name": "tbm-temps-reel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Site TBM temps reel + outils d'archivage/analyse du flux GTFS-RT.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python -m http.server 8080",
    "record": "node tools/record-feed.mjs"
  },
  "dependencies": {
    "protobufjs": "^7.3.2"
  }
}
